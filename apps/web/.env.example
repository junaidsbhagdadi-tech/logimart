# Base URL of the Logimart ERP API — ONLY needed for split local dev
# (Vite portal on :5180 talking to the API on :3000).
#
# In production leave this UNSET: the API serves the built portal same-origin,
# so the portal calls /api/v1/... on the same host.
VITE_API_URL=http://localhost:3000
